change .env.production to real url backend

change vite.config.js to real repository

Your backend at mybackend.com must allow requests from your GitHub Pages URL (https://YOUR-USERNAME.github.io). Add that origin to your backend's CORS allowed list.


Go to your repo on GitHub → Settings → Pages
Under Source, select Deploy from a branch
Set branch to gh-pages, folder to / (root) → Save

Your backend at mybackend.com must allow requests from your GitHub Pages URL (https://YOUR-USERNAME.github.io). Add that origin to your backend's CORS allowed list.

